# MIDI Commander Custom Firmware Walkthrough

このドキュメントでは、カスタムファームウェアの導入から設定変更までの手順を解説します。

## 1. ファームウェアのインストール

ビルド済みのDFUファイルを書き込むだけで、すべての機能が利用可能になります。

### 必要なもの

- `dfu-util` (または ST Microelectronics 製 DFU ツール)
- 生成されたDFUファイル: `artifacts/dfu/platformio-latest.dfu`

### 書き込み手順

1. **DFUモード起動**: Midi Commander の `Bank Down` + `D` ボタンを押しながら電源を入れます（画面は消えたまま、LED3が点灯）。
2. **書き込みコマンド実行**:
   ```bash
   dfu-util --alt 0 --download artifacts/dfu/platformio-latest.dfu
   ```
3. 電源を入れ直すと、カスタムファームウェアが起動します。

---

## 2. 設定の変更（ビルド不要）

本ファームウェア最大の特徴は、**CSVファイルを書き換えるだけで、スイッチの割当やエクスプレッションペダルのCC番号を変更できる** 点です。毎回ソースコードを書き換えてビルドする必要はありません。

### 手順

1. **PCと接続**:
   Midi Commander を**通常モード**（DFUではない）で起動し、USBでPCに接続します。

2. **設定ファイル (CSV) の編集**:
   `python/MeloConfig_10_Cmds - RC-600.csv` をテキストエディタやExcelで開きます。

   **エクスプレッションペダルの設定**:
   Global_Settings セクションにある以下の数値を変更します。

   ```csv
   Exp1_CC,11    <-- EXP1 の CC番号 (0-127)
   Exp2_CC,4     <-- EXP2 の CC番号 (0-127)
   ```

   **スイッチの設定**:
   下部の `Button_Settings` テーブルを編集して、各スイッチに送信させたいMIDIコマンド（CC, PC, Noteなど）を自由に割り当てます。

3. **設定の転送**:
   Pythonツールを使って、CSVの内容を実機に転送します。

   ```bash
   # Pythonライブラリのインストール（初回のみ）
   pip install -r python/requirements.txt

   # 設定の書き込み
   python python/CSV_to_Flash.py "python/MeloConfig_10_Cmds - RC-600.csv"
   ```

   実行すると `Writing Flash Chunk...` と表示され、完了後に自動的に実機が再起動して新しい設定が反映されます。

---

## 3. 今回の改善点（技術詳細）

### エクスプレッションペダル (v_exp_final)

本バージョンでは、エクスプレッションペダルの挙動を抜本的に改善しました。

- **デュアル入力完全対応**: EXP1 / EXP2 両方を同時に使用可能です。
- **物理クロストーク/チャージアップ防止 (Pull-Down Switching)**:
  - 未使用時のピンを GPIO Pull-Down で強力に GND に固定し、ノイズの蓄積（チャージアップ）を物理的に阻止します。
  - 測定する瞬間だけアナログモードに切り替え、測定が終わると即座に GND に落とす独自ロジックを実装しました。
- **高インピーダンス対応 (Long Recovery Time)**:
  - 測定前に十分な電圧回復時間 (約1ms) を確保することで、高インピーダンスな民生用ペダルでも電圧降下を起こさず、最大値 (127) まで確実に到達するようにしました。
- **アダプティブフィルタ (Adaptive Filter)**:
  - 静止時は強力な平滑化で値を安定させ、操作時はフィルタを弱めて高速追従させることで、「安定性」と「低レイテンシ」を両立しました。
- **デッドゾーン最適化**:
  - 0付近と127付近に適切な不感帯を設け、確実に最小・最大値が出るように調整しました。

### 設定の柔軟性

- エクスプレッションペダルのCC番号をハードコード（固定）から、Flashメモリ読み出し方式に変更しました。
- これにより、上記の Python ツール経由で自由に番号を変更可能になりました。
